# 何を: peer dependency の不一致を許容する
# なぜ: vite-plugin-pwa@1.2.0 の peer は Vite ^7 までだが、
#       本プロジェクトは Vite 8 を採用している（plugin API は互換）。
#       npm ci がデフォルトでは ERESOLVE で失敗するため、ローカル/CI両方で許容する。
legacy-peer-deps=true
